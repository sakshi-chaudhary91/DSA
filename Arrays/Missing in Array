class Solution {
    int missingNum(int arr[]) {
        long n = arr.length + 1;
        long totalSum = n * (n + 1) / 2;
        long arraySum = 0;
        for (int num : arr) {
            arraySum += num;
        }

        return (int)(totalSum - arraySum);
    }
}
